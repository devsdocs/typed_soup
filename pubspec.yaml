name: typed_soup
description: Dart native package inspired by Beautiful Soup 4 Python library. Provides easy ways of navigating, searching, and modifying the HTML tree.
homepage: https://github.com/devsdocs/typed_soup
version: 1.0.1

environment:
  sdk: ^3.12.0

dependencies:
  html: ^0.15.6

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.15
